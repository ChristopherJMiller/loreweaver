/**
 * get_campaign_context MCP Tool
 *
 * Get high-level campaign context including:
 * - Campaign details (name, system, description)
 * - Active quests
 * - Heroes (player characters)
 * - Root locations (world/continent level)
 *
 * Aggregates data from multiple Tauri commands:
 * - get_campaign
 * - list_quests (filtered to active)
 * - list_heroes (filtered to active)
 * - list_locations (filtered to root level)
 */

// TODO: Implement once @anthropic-ai/claude-agent-sdk is installed (#60)

export const getCampaignContextTool = null; // Placeholder
